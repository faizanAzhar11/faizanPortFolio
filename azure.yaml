# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: faizan-portfolio
metadata:
  template: faizan-portfolio@0.0.1-beta

services:
  web:
    project: .
    language: ts
    host: staticwebapp
    dist: out

hooks:
  postdeploy:
    shell: sh
    run: |
      echo "Portfolio deployed successfully!"
      echo "Static Web App URL: $AZURE_STATIC_WEB_APP_URL"
